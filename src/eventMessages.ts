/**
 * Fired when app-ready.
 */
export const EVT_APP_READY = "ready";

/**
 * Fired when app-close.
 */
export const EVT_APP_CLOSE = "close";

/**
 * Fired when user is tring to close app.
 */
export const EVT_CLOSING = "closing";

/**
 * Fired when app tries to show source selector.
 */
export const EVT_SHOW_SELECTOR = "showSourceSelector";

/**
 * Fired when user decided video source.
 */
export const EVT_SRC_SELECTED = "EVT_SRC_SELECTED";
